# 5. **Count and Index in Tuple**: Use `count()` and `index()` methods on a tuple to find the occurrences and positions of elements.


tuple1 = (1,4,5,5,2, 3)
for i in tuple1:
    print(f"{i} is {tuple1.count(i)} times in the list and the index is {tuple1.index(i)}.")


